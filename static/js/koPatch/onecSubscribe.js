// Generated by CoffeeScript 1.9.3
(function() {
  ko.observable.fn.onecSubscribe = function(callback, callbackTarget, event) {
    var boundCallback, self, subscription;
    self = this;
    if (!self.isOnecSubscribe) {
      event = event || "change";
      boundCallback = callbackTarget ? callback.bind(callbackTarget) : callback;
      subscription = new ko.subscription(self, boundCallback, function() {
        ko.utils.arrayRemoveItem(self._subscriptions[event], subscription);
        if (self.afterSubscriptionRemove) {
          return self.afterSubscriptionRemove(event);
        }
      });
      if (self.beforeSubscriptionAdd) {
        self.beforeSubscriptionAdd(event);
      }
      if (!self._subscriptions[event]) {
        self._subscriptions[event] = [];
      }
      self._subscriptions[event].push(subscription);
      self.isOnecSubscribe = true;
      return subscription;
    } else {
      return self;
    }
  };

}).call(this);

//# sourceMappingURL=onecSubscribe.js.map
